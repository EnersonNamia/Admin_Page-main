# Environment variables for Course Recommendation System

# Database Configuration
DB_HOST=mainline.proxy.rlwy.net
DB_PORT=39307
DB_NAME=railway
DB_USER=postgres
DB_PASSWORD=yPgibLgnWCqZYiHXqQIweIbmETqseqYE

# Server Configuration
PORT=5000
ENVIRONMENT=development

# Frontend URL
FRONTEND_URL=http://localhost:3000

# JWT Secret (Generate a secure secret in production)
# IMPORTANT: Change this to a secure random string before deploying!
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=95b81ac416d219dc2783b7d2d65fbc9fb032d44635dbe37f550dea5f35921264
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Rate Limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW_MINUTES=15

# CORS Origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002,http://127.0.0.1:3000,http://127.0.0.1:3001,http://127.0.0.1:3002

# Email Configuration (Gmail SMTP)
# To get an App Password: https://myaccount.google.com/apppasswords
# 1. Enable 2-Factor Authentication on your Google account
# 2. Go to App Passwords and create one for "Mail" on "Windows Computer"
# 3. Copy the 16-character password here (without spaces)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=namianamia77@gmail.com
SMTP_PASSWORD=goegfieoznrfnemz
ADMIN_EMAIL=namianamia77@gmail.com
